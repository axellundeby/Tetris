package no.uib.inf101.grid;
/**

 * A recordclass that represents a possition on a grid
 * @param row a given row
  * @param cal a given columns 
 */
public record CellPosition(int row, int col) {
    
}
